var LightStick = LightStick || {};

LightStick.Router = Backbone.View.extend({
    initialize: function() {
        this.el = options.el;
        console.log("View", this.el);
    },

    render: function() {

    }
});
